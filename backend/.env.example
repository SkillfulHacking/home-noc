# backend/.env.example
# Copy to .env and override as needed

# Security: if set, all write endpoints require this API key via X-API-Key
API_KEY=changeme-please-32bytes-min

# CORS (comma-separated origins). Leave empty to disable CORS.
CORS_ORIGINS=

# Database: default local SQLite under /data
DATABASE_URL=sqlite:////data/home_noc.db

# App
APP_NAME=home-noc
APP_ENV=prod
